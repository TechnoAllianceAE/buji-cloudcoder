// Stub — usage monitoring handled by enterprise gateway in BujiCoderPlus
export function useUsageMonitor(_params?: Record<string, unknown>) {
  return {
    isOverLimit: false,
    usage: null,
  }
}
